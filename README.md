# erichung.dev

Personal site. Astro (static), two modes (Developer / Dancer), deployed on Netlify.

## Develop
    npm install
    npm run dev

## Check / test
    npm run check
    npm run test
    npm run test:e2e

## Deploy
Push to `main`. Netlify builds `npm run build` and publishes `dist/`.
